<resources>
    <!-- Example customization of dimensions originally defined in res/values/dimens.xml
         (such as screen margins) for screens with more than 820dp of available width. This
         would include 7" and 10" devices in landscape (~960dp and ~1280dp respectively). -->
    <dimen name="activity_horizontal_margin">64dp</dimen>
    <dimen name="searchPagePadding">10dp</dimen>

    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_vertical_margin">16dp</dimen>

    <!-- Padding info for each quake view @id/addressText -->
    <dimen name="paddingTop"> 10dp </dimen>


    <!-- Padding info for the @id/timeText -->
    <dimen name="paddingDateTimeRight">8dp</dimen>
    <dimen name="paddingDateTime">5dp</dimen>
    <dimen name="paddingDateTimeTop"> 10dp </dimen>

    <!-- paddng top for @id/timeText -->
    <dimen name="paddingTimeTop">2dp</dimen>
    <dimen name="fab_margin">16dp</dimen>

    <!-- Elevation for the toolbar -->
    <dimen name="toolbarElevation">5dp</dimen>
</resources>
